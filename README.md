# Vanilla JS Sketchpad

## To-do:
- Fill while mouse is dragging across the sketchpad.
- Resize feature for grid (refactor CSS grid settings).
- Erase feature.
- Pretty up the page itself and center elements.